image-rs: The loaders and editors now use gufo 0.5. This brings support for more metadata extraction like key-value medata from PNGs and `<metadata>` entries from SVGs. For editing, this now supports changing the orientation of images with a broken Exif orientation tag.
